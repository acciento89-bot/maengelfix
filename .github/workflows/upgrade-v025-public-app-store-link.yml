name: MängelFix Public App Store Link
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/upgrade-v025-public-app-store-link.yml
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/upgrade_v025_public_app_store_link.py
      - name: Build web app
        working-directory: client
        run: |
          npm install
          npm run build
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add client/src/App.jsx
          if git diff --cached --quiet; then
            echo "Nothing to commit"
          else
            git commit -m "Add public App Store download link"
            git push
          fi
