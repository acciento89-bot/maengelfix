name: MängelFix App Store Support Upgrade
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/upgrade-v024-app-store-support.yml
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/upgrade_v024_app_store_support.py
      - name: Build web app
        working-directory: client
        run: |
          npm install
          npm run build
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add client/src/App.jsx
          if git diff --cached --quiet; then
            echo "Nothing to commit"
          else
            git commit -m "Add App Store support page"
            git push
          fi
