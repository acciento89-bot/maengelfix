name: MängelFix v0.16 Beta Hardening
on:
  push:
    branches: [main]
    paths: ['.github/workflows/upgrade-v016.yml']
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: python3 scripts/upgrade_v016_beta_hardening.py
      - run: node --check server/index.js
      - run: |
          cd client
          npm install --no-audit --no-fund
          npm run build
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server/index.js server/package.json client/src/App.jsx client/src/maengelfix-pro.css
          if git diff --cached --quiet; then echo "Nothing to commit"; else git commit -m "Harden MängelFix v0.16 export legal and tenant flows" && git pull --rebase origin main && git push; fi
